# Normalize line endings: LF in the repo, checked out native. Keeps templ/gofmt
# output stable across Windows/Linux and silences CRLF warnings.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.gz binary
*.db binary
